{
  "name": "@repo/geometry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"@repo/geometry: jit package, no build\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"skip\""
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "polygon-clipping": "^0.15.7"
  }
}
