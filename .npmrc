auto-install-peers=true
strict-peer-dependencies=false
# pnpm 심볼릭 레이아웃 + Vite dep optimize 조합에서 react/react-dom이 중복 인스턴스로 묶여
# RR v7 dev가 "useContext of null"로 깨진다. flat 노드모듈로 단일 인스턴스를 보장한다.
node-linker=hoisted
